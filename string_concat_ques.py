# user will enter there name. I have to return hello user

name = input("Enter name: ")
print("hello "+name)

# user will enter a string. We have to verify, if the string consists of only
# digits. Also print length of string

a = input("Enter string: ")
print(a.isdigit())
print(len(a))

# 
